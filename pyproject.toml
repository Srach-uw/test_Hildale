[tool.pytest.ini_options]
testpaths = ["scripts", "tests"]
python_files = ["test_*.py"]
norecursedirs = [
    ".git",
    ".pytest_cache",
    ".venv",
    "legacy",
    "reference",
    "tmp",
]
addopts = "-ra"
